public class FixedSlidingWindow {
    

    // Here we will have the size of the window
    // Then if the window moves forward, then we will remove the last element of the window too.
    // We will now perform more questions for this topics
    
}
