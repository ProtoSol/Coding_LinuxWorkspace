public class IntroToHashing {
    // Storing the values in pair of key:value
    // this is achieved via Hashmap, Hashset, Linked Hashing, Tree Map

    // Hashmap came after 1.4 but before Hashtable is made
    // This is unordered in nature

    // Hash set only stores unique value and It's occurances.
}
