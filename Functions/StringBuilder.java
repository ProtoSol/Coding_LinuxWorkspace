public class StringBuilder {
    public static void main(String[] args) {
        StringBuilder sb = new StringBuilder();
        // StringBuffer sbu = new StringBuffer();

        // sbu.append("Hello There");
        // System.out.println(sbu.capacity());
        // System.out.println(sbu.length());
        // sbu.append("Lets check again");
        // System.out.println(sbu.capacity());
        // System.out.println(sbu.length()); 
        // sbu.ensureCapacity(100);
        
        // // String builder is not synchronized to make it synchronous we use synchronous lock.
        // synchronized(sb){
        //     sb.append("Hi");
        // }

    }
}
